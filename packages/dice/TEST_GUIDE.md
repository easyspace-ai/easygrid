// 手动测试指南
// 使用浏览器打开 http://localhost:5173 并按照以下步骤测试：

console.log(`
=== 列拖动排序和右键菜单功能测试指南 ===

1. 拖拽功能测试：
   - 在浏览器中打开 http://localhost:5173
   - 找到表格的列头（例如 "Difficulty", "Category" 等）
   - 点击并拖动列头的文本区域（不是图标）
   - 应该看到：
     ✓ 半透明的灰色阴影跟随鼠标移动
     ✓ 阴影高度为整个表格的高度
     ✓ 被覆盖的列会高亮边线（左边或右边）
     ✓ 列只能在横向移动，Y轴锁定在表头行
     ✓ 释放鼠标后，列顺序应该改变

2. 右键菜单功能测试：
   - 右键点击任意列头
   - 应该看到：
     ✓ 下拉菜单出现在右键点击的位置
     ✓ 菜单包含：Sort asc, Sort desc, Pin to left, Pin to right, Hide column
     ✓ 菜单位置正确（x, y 轴都在点击位置附近）

3. 交互冲突测试：
   - 左键点击列头文本区域：应该触发拖拽，不应该打开菜单
   - 右键点击列头：应该打开菜单，不应该触发拖拽
   - 点击下拉菜单图标：菜单已移除，现在只有右键触发

=== 测试完成 ===
`);

